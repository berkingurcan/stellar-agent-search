{
  "name": "@stellar-agent-mcp/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit --strict --skipLibCheck --target ES2022 --module ESNext --moduleResolution Bundler --resolveJsonModule --allowSyntheticDefaultImports --lib ES2022,DOM,DOM.Iterable --types node,vitest/globals src/index.ts test/*.test.ts",
    "test": "vitest run test",
    "dry-run": "wrangler deploy --dry-run --outdir .wrangler-ci",
    "canary": "node scripts/canary.mjs",
    "predeploy": "node scripts/validate-deploy-config.mjs wrangler.jsonc",
    "deploy": "wrangler deploy"
  }
}
