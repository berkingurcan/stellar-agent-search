export type InstallConfig = {
	id: string;
	label: string;
	/** One-line description shown next to the client's glyph in the picker. */
	tagline: string;
	lang: string;
	code: string;
	note: string;
};
