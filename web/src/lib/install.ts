/**
 * Release switch: keep the published payload out of the module graph until the
 * post-publish verification runbook is complete. Change only this re-export.
 */
export * from './install-published.js';
